import Vue from 'vue'
import Vuex from 'vuex'
import MainWindow from './modules/MainWindow';

Vue.use(Vuex);

export default new Vuex.Store({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        MainWindow
    }
});
